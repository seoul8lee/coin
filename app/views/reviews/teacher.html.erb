<div>
    <table>
    <tr>
    <th>제목</th>
    </tr>
    <% @reviews.each do |review| %>
    <tr>
    <td><%= link_to "/reviews/show/#{review.id}" do%><%= review.title %><%end%></td>
    </tr>
    <% end %>
</table>
</div>

