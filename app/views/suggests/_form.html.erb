<%= form_for(@suggest) do |f| %>
  <% if @suggest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggest.errors.count, "error") %> prohibited this suggest from being saved:</h2>

      <ul>
      <% @suggest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-md-8 push-md-2 col-sm-10 push-sm-1 mb-5 ">
    <%= f.label :title, "제목", class: "control-label" %><br>
    <%= f.text_field :title, class: "form-control" %>
  </div>
  <div class="form-group col-md-8 push-md-2 col-sm-10 push-sm-1 mb-5 ">
    <%= f.label :content, "내용", class: "control-label" %><br>
    <%= f.text_area :content, class: "form-control" %>
  </div>
  <div class="form-group col-md-8 push-md-2 col-sm-10 push-sm-1 mb-5 ">
    <%= f.submit %>
  </div>
<% end %>

